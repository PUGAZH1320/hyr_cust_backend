export const errorCodes = {
  resOk: 200,
  resCreated: 201,
  resBadResponse: 400,
  resUnauth: 401,
  resForbid: 403,
  resNotFound: 404,
  resIntError: 500,
};

export enum SupportedLanguages {
  EN = "en",
  IT = "it",
}

export enum StatusCode {
  Ok = 200,
  Created = 201,
  BadRequest = 400,
  UnAuthorized = 401,
  Forbidden = 403,
  NotFound = 404,
  InternalServerError = 500,
}
